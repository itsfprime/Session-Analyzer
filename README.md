# Session-Analyzer
A simple Java application which statistically interprets speedcubing session data exported from CSTimer.net

IMPORTANT:
The only way to change between session files is by using an IDE or other code editor. Will be addressed later.
Session data exported from CSTimer needs to have the first line removed before this program will read it. This can be done by manually
editing the .csv file and deleting the first line entirely. 
